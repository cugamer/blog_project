<%= form_for :comment, url: comment_path do |f| %>
  <%= render 'shared/error_messages' %>
  
  <%= f.label "Enter comment" %>
  <%= f.text_area :comment %>
  
  <%= f.submit "Submit comment" %>

<% end %>